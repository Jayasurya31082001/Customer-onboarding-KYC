<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %-40.40logger{39} – %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>target/cucumber-reports/test-automation.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>target/cucumber-reports/test-automation-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %-40.40logger{39} – %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress RestAssured verbose output unless DEBUG is explicitly enabled -->
    <logger name="io.restassured" level="WARN"/>
    <logger name="org.apache.http" level="WARN"/>
    <logger name="groovyx.net.http" level="WARN"/>

    <!-- Our suite logs at INFO -->
    <logger name="com.kyc.automation" level="INFO"/>

    <root level="WARN">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
