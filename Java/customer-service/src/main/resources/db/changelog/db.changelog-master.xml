<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="1-create-customers-table" author="copilot">
        <createTable tableName="customers">
            <column name="customer_id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="first_name" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="last_name" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="email" type="VARCHAR(255)">
                <constraints nullable="false" unique="true" uniqueConstraintName="uk_customers_email"/>
            </column>
            <column name="date_of_birth" type="DATE">
                <constraints nullable="false"/>
            </column>
            <column name="phone_number" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="nationality" type="VARCHAR(2)">
                <constraints nullable="false"/>
            </column>
            <column name="address_line1" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="city" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="postcode" type="VARCHAR(10)">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(30)">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!--
        Changeset 2 — Add updated_at column for incremental sync support.

        The data-sync-service uses updated_at as the sync cursor to detect
        changes since the last run. This column is managed automatically by
        Spring Data JPA auditing (@LastModifiedDate) via @PreUpdate on the entity.

        Back-fill: existing rows get updated_at = created_at so the column
        is never null and the first sync picks them all up correctly.
    -->
    <changeSet id="2-add-updated-at-to-customers" author="data-sync-service">
        <addColumn tableName="customers">
            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>
        </addColumn>

        <!-- Back-fill existing rows: set updated_at = created_at -->
        <update tableName="customers">
            <column name="updated_at" valueComputed="created_at"/>
            <where>updated_at IS NULL</where>
        </update>

        <!-- Create index on updated_at to make incremental queries efficient -->
        <createIndex tableName="customers" indexName="idx_customers_updated_at">
            <column name="updated_at"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>

